// Add to tacit/build.sbt — lazy val lib project, libraryDependencies:

// Local publish first:  cd zio-pdf && sbt publishLocal
// Or depend on a published artifact when available:
libraryDependencies += "io.github.adrielc" %% "zio-pdf" % "0.1.0-SNAPSHOT"
